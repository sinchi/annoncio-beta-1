/*var customColorScheme = function (user) {
 if(user.status)
 	return "#5cb85c";
 else
 	return "#d9534f";
};*/
// green #5cb85c
// red #d9534f

Avatar.setOptions({
  imageSizes: {
    'extra-large': 200,
    'extra-small': 20
  }/*,
  backgroundColor: customColorScheme*/
});